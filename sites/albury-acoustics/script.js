const toggle=document.querySelector('.nav-toggle'),nav=document.querySelector('.site-nav');function closeNav(){if(!toggle||!nav)return;toggle.setAttribute('aria-expanded','false');toggle.querySelector('.sr-only').textContent='Open navigation';nav.classList.remove('is-open');document.body.classList.remove('nav-open')}if(toggle&&nav){toggle.addEventListener('click',()=>{const open=toggle.getAttribute('aria-expanded')!=='true';toggle.setAttribute('aria-expanded',String(open));toggle.querySelector('.sr-only').textContent=open?'Close navigation':'Open navigation';nav.classList.toggle('is-open',open);document.body.classList.toggle('nav-open',open)});nav.addEventListener('click',closeNav);document.addEventListener('keydown',event=>{if(event.key==='Escape')closeNav()})}document.querySelectorAll('[data-year]').forEach(node=>node.textContent=new Date().getFullYear());const reveals=document.querySelectorAll('[data-reveal]');if('IntersectionObserver'in window&&!matchMedia('(prefers-reduced-motion: reduce)').matches){const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('is-visible');observer.unobserve(entry.target)}}),{threshold:.12});reveals.forEach(node=>observer.observe(node))}else reveals.forEach(node=>node.classList.add('is-visible'));
const productButtons=document.querySelectorAll('[data-product-filter]'),productCards=document.querySelectorAll('[data-product-grid] .aa-product-card');productButtons.forEach(button=>button.addEventListener('click',()=>{productButtons.forEach(item=>item.classList.toggle('is-active',item===button));const filter=button.dataset.productFilter;productCards.forEach(card=>card.hidden=filter!=='all'&&card.dataset.division!==filter)}));const aaSubject=document.querySelector('[data-aa-subject]');if(aaSubject){const params=new URLSearchParams(location.search);aaSubject.value=params.get('product')||params.get('service')||''}
(()=>{const s=document.createElement('script');s.src=new URL('../group-switcher.js',document.currentScript.src);s.dataset.currentSite='albury';document.head.append(s)})();
